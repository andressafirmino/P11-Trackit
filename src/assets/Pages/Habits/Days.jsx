import styled from "styled-components";

export default function Days(props) {
   const {day, select, i, setSelect} = props;

   
    
    
}

const Day = styled.button `
   width: calc(10% - 80px);
   height: 30px;
`
